title: Templates
icon: fieldtype-template
revisions: false
entry_class: MarioHamann\StatamicVisualEditor\CollectionTemplateEntry
# Deliberately NO route. Live Preview uses /!/sve/collection-view-preview/{id}
# (attached by the addon when collection_templates is on). These are view files'
# CP entries, never public pages.
preview_targets:
  - label: Template
    url: '/!/sve/collection-view-preview/{id}'
    refresh: true
